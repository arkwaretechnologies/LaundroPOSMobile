# Supabase Configuration
# Copy this file to .env and fill in your actual values
# Never commit .env to version control

# Supabase Project URL
EXPO_PUBLIC_SUPABASE_URL=your_supabase_project_url

# Supabase Anonymous/Public Key (Client-side key)
EXPO_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key

# JWT Secret (Legacy/Server-side secret - typically not used in client apps)
# This is the JWT secret configured in Supabase Dashboard > Authentication > Settings
# Note: This is primarily for reference/documentation. JWT secrets are server-side only.
# The client app uses the anon key above, not the JWT secret.
JWT_SECRET=IYHgY2ETv+do4wL5iwER9G0woYtimhaAaIsdv/ytuVd6cGnF0Q/HSHycj3OcghnvHVis437fGMLWW+SCU/dADg==
